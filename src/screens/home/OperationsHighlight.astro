---
const images = [
  {
    src: "/images/operaciones-recoleccion",
    alt: "Equipo realizando la recolección de residuos industriales",
    caption: "Operaciones de Recolección Eficientes"
  },
  {
    src: "/images/compromiso-ambiental",
    alt: "Prácticas sostenibles en el manejo de residuos",
    caption: "Compromiso con el Medio Ambiente"
  }
];
---
<section class="py-5" style="background: #f8f9fa;">
  <div class="container text-center">
    <!-- Título con efecto de brillo -->
    <h2 class="fw-bold text-danger mb-3 title-glow">Nuestras Operaciones</h2>
    <p class="text-muted mb-5">Comprometidos con la excelencia y el medio ambiente</p>

    <div class="row">
      {images.map((image) => (
        <div class="col-md-6 mb-4">
          <div class="card border-0 shadow-lg card-custom">
            <img src={image.src} alt={image.alt} class="card-img-top img-fluid rounded image-small" />
            <div class="card-body text-white text-center">
              <p class="card-text fw-bold">{image.caption}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
/* ✅ Efecto de brillo en el título */
.title-glow {
  text-shadow: 0px 0px 10px rgba(255, 255, 255, 0.8);
}

/* ✅ Personalización de tarjetas */
.card-custom {
  background: linear-gradient(145deg, #b3001b, #ee2b47, #d5112d);
  border-radius: 15px;
  overflow: hidden;
  transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
}

/* ✅ Sombra y efecto hover en tarjetas */
.card-custom:hover {
  transform: scale(1.05);
  box-shadow: 0px 8px 16px rgba(255, 255, 255, 0.2);
}

/* ✅ Ajuste de textos */
.card-body {
  background: rgba(0, 0, 0, 0.5); /* Fondo oscuro semitransparente */
  padding: 15px;
  border-radius: 0 0 15px 15px;
}

/* ✅ Reducir tamaño de imágenes */
.image-small {
  max-height: 250px;
  object-fit: cover;
}
</style>