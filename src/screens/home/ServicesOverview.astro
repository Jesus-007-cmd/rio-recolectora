
---
const services = [
  { title: "Recolección de Residuos", description: "Manejo eficiente y responsable de residuos industriales y urbanos." },
  { title: "Residuos de Manejo Especial", description: "Gestión especializada para residuos con regulaciones específicas." },
  { title: "Destrucción Fiscal", description: "Eliminación segura de documentos y productos para su disposición final." },
  { title: "Reciclaje y Acopio", description: "Fomento del reciclaje y valorización de materiales reutilizables." }
];

const carouselItems = [
  { imageUrl: "/images/carrusel-principal/img1.jpg" },
  { imageUrl: "/images/carrusel-principal/img2.jpg" },
  { imageUrl: "/images/carrusel-principal/img3.jpg" },
  { imageUrl: "/images/carrusel-principal/img4.jpg" }
];
---
<section id="services" class="py-5" style="background: radial-gradient(#d5112d, rgb(198, 86, 103)) !important;">
  <div class="container">
    <div class="row align-items-center">
      <!-- Imagen a la izquierda con carrusel -->
      <div class="col-md-5">
        <div id="carouselExample" class="carousel slide" data-bs-ride="carousel" data-bs-interval="3000">
          <div class="carousel-inner">
            {carouselItems.map((item, index) => (
              <div class={`carousel-item ${index === 0 ? 'active' : ''}`} key={index}>
                <img src={item.imageUrl} class="d-block w-100" alt="Imagen de servicio" style="height: 400px; object-fit: fill; border-radius: 10px;" />
                <div class="carousel-caption d-block d-md-none" style="background: rgba(0, 0, 0, 0.6); border-radius: 10px; padding: 15px;">
                  <h5 style="color: #fff; font-weight: bold;">{services[index]?.title}</h5>
                  <p style="color: #ddd; font-size: 14px;">{services[index]?.description}</p>
                </div>
              </div>
            ))}
          </div>
          <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
          </button>
        </div>
      </div>
      <!-- Textos a la derecha solo visibles en PC -->
      <div class="col-md-7 text-center d-none d-md-block">
        <div class="row g-4">
          {services.map((service, index) => (
            <div class="col-md-6" key={index}>
              <div class="p-4" style="background-color: #ffffff; border-radius: 10px; box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);">
                <h4 class="fw-bold" style="color: #d32f2f;">{service.title}</h4>
                <p class="text-dark">{service.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>