<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tarjeta Giratoria</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Estilos de la tarjeta giratoria */
    .perspective-1000 {
      perspective: 1000px;
    }

    .card-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 180px;
      
    }

    .card {
      width: 400px;
      height: 200px;
      perspective: 1000px;
    }

    .card-inner {
      position: relative;
      width: 100%;
      height: 100%;
      transform-style: preserve-3d;
      transition: transform 1s;
    }

    .card-front, .card-back {
      position: absolute;
      width: 100%;
      height: 100%;
      backface-visibility: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 1.25rem;
      font-weight: bold;
      text-align: center;
      color: #3e2723;
    }

    .card-front {
      background-color: #ffeb3b; /* Amarillo */
    }

    .card-back {
      background-color: #ffeb3b; /* Amarillo */
      transform: rotateY(180deg); /* Reverso con rotación */
    }

    /* Animación para girar la tarjeta */
    .flip {
      transform: rotateY(180deg);
    }

    /* Efecto de animación para mostrar las tarjetas */
    .card-container .card-inner {
      animation: flipCard 10s ease-in-out infinite; /* Aumentar duración a 10s */
    }

    @keyframes flipCard {
      0% {
        transform: rotateY(0deg); /* Empieza en la posición original */
      }
      50% {
        transform: rotateY(180deg); /* Gira 180 grados, mostrando el reverso */
      }
      100% {
        transform: rotateY(0deg); /* Regresa a la posición original */
      }
    }
  </style>
</head>
<body>

  <div class="card-container">
    <div class="card perspective-1000">
      <div class="card-inner">
        <!-- Frente de la tarjeta -->
        <div class="card-front bg-yellow-400 text-gray-800 center">
          CUIDADO ANTES DE CONTRATAR ...
        </div>

        <!-- Reverso de la tarjeta -->
        <div class="card-back bg-yellow-400 text-gray-800">
          Recuerde que a quien contrate tendrá acceso a su casa, coto, fraccionamiento, negocio, establecimiento, empresa, NO SEA VÍCTIMA DE 'HALCONEO' DEL CRIMEN ORGANIZADO, contrate empresas recolectoras formales.
        </div>
      </div>
    </div>
  </div>

</body>
</html>
