<header>
  <nav class="navbar navbar-expand-lg fixed-top navbar-custom shadow">
    <div class="container d-flex justify-content-between align-items-center">
      
      <!-- Logo y Nombre de la Empresa -->
      <div class="d-flex align-items-center w-100">
        <a class="navbar-brand" href="/">
          <img src="/images/logo.png" alt="RIO Recolectora" class="navbar-logo img-fluid">
        </a>
        <div class="company-name ms-2 text-white">
          <h1 class="mb-0 fw-bold">Recolectora Industrial de Occidente</h1>
          <p class="mb-0">Recolección de Residuos de Manejo Especial</p>
        </div>
      </div>

      <!-- Información de Contacto -->
      <div class="d-none d-lg-flex flex-column align-items-end text-white">
        <span class="fw-bold fs-5">TEL. 3810 · 0453</span>
        <span class="small">Calle 12 Nº2001, Z.I. Guadalajara, Jalisco.</span>
      </div>
    </div>
  </nav>
</header>

<style>
/* ✅ Fondo de la navbar con degradado basado en la imagen */
.navbar-custom {
  background: linear-gradient(145deg, #ee2b47, #d5112d, #f25a70, #b3001b) !important;
  padding: 15px 0;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

/* ✅ Tamaño del logo con animación de brillo */
.navbar-logo {
  max-height: 90px;
  filter: drop-shadow(0px 0px 10px rgba(255, 255, 255, 0.8)); /* Efecto de brillo */
  animation: glowEffect 3s infinite alternate ease-in-out;
  transition: transform 0.3s ease-out;
}

/* ✅ Animación de brillo para el logo */
@keyframes glowEffect {
  0% {
    filter: drop-shadow(0px 0px 5px rgba(255, 255, 255, 0.5));
  }
  50% {
    filter: drop-shadow(0px 0px 20px rgba(255, 255, 255, 1));
  }
  100% {
    filter: drop-shadow(0px 0px 5px rgba(255, 255, 255, 0.5));
  }
}

/* ✅ Efecto de giro rápido al pasar el mouse */
.navbar-logo:hover {
    transition: transform 4s ease-out;
  transform: rotateY(360deg)
} 

/* ✅ Ajuste de texto */
.company-name h1 {
  font-size: 2rem;
  color: #fff;
}

.company-name p {
  font-size: 1.2rem;
  color: #fff;
}

/* ✅ Contacto alineado a la derecha */
.d-lg-flex span {
  color: #fff;
}

/* ✅ Sombra para mejorar legibilidad en el texto */
.text-white {
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
}
</style>
