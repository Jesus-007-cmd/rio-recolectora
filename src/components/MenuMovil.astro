<!-- Menú de móvil (solo visible en pantallas pequeñas) -->
<div class="mobile-menu-container">
  <input type="checkbox" id="hamburger-toggle" class="hamburger-toggle">
  <label for="hamburger-toggle" class="hamburger-menu">☰</label>
  
  <div class="mobile-menu">
    <!-- Elementos del menú -->
    <a class="mobile-menu-item" href="/#about">Acerca de</a>
    <a class="mobile-menu-item" href="/#services">Servicios</a>
    <a class="mobile-menu-item" href="/#operations">Operaciones</a>
    <a class="mobile-menu-item" href="/#why-choose-us">¿Por qué elegirnos?</a>
    <a class="mobile-menu-item" href="/#testimonials">Testimonios</a>
    <a class="mobile-menu-item" href="/#clients">Clientes</a>
  </div>
</div>

<style>
  /* Estilos solo para dispositivos móviles */
  @media (max-width: 768px) {
    .mobile-menu-container {
      position: fixed;
      bottom: 20px;
      right: 20px;
      z-index: 1000;
    }

    /* El botón de hamburguesa (checkbox oculto) */
    .hamburger-toggle {
      display: none;
    }

    .hamburger-menu {
      width: 60px;
      height: 60px;
      background: linear-gradient(135deg, #c62828, #d32f2f);
      border: none;
      border-radius: 50%;
      color: white;
      font-size: 32px;
      font-weight: bold;
      text-align: center;
      cursor: pointer;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 0;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }

    .mobile-menu {
      display: none;
      position: absolute;
      bottom: 70px;
      right: 0;
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      padding: 10px;
      min-width: 180px;
    }

    .hamburger-toggle:checked + .hamburger-menu + .mobile-menu {
      display: block; /* Muestra el menú cuando se activa el checkbox */
    }

    .mobile-menu-item {
      display: block;
      padding: 10px 15px;
      text-decoration: none;
      color: #c62828;
      font-weight: bold;
    }

    .mobile-menu-item:hover {
      background: #f5f5f5;
    }

    /* Estilos para los submenús */
    .submenu {
      display: none;
      padding-left: 20px;
    }

    .submenu-toggle:checked + .submenu {
      display: block; /* Muestra el submenú cuando se activa el checkbox */
    }

    .submenu-toggle-label {
      cursor: pointer;
      margin-left: 10px;
    }

    .submenu a {
      display: block;
      padding: 8px 0;
      text-decoration: none;
      color: #333;
    }

    .submenu a:hover {
      background: #f5f5f5;
    }
  }
</style>
